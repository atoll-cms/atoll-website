# Default Theme

Place Twig overrides in `themes/default/templates/`.

Loader precedence in atoll-cms:
1. `templates/` (site-level override)
2. `themes/<active-theme>/templates/`
3. `core/themes/default/templates/` (core fallback)
