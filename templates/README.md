# Site Template Overrides

Place project-specific Twig overrides here.

Resolution order for template lookup:
1. `templates/` (project overrides)
2. `themes/<active-theme>/templates/`
3. `core/themes/default/templates/` (core fallback)
