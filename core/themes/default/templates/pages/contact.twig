{% extends 'layouts/base.twig' %}

{% block content %}
  <h1>{{ page.title }}</h1>
  <article class="prose">{{ page.content|raw }}</article>

  <section>
    <h2>Kontaktformular</h2>
    {{ island('ContactForm', { client: 'visible', props: { endpoint: url('/forms/contact'), csrf: csrf_token } })|raw }}
  </section>
{% endblock %}
