<IfModule mod_rewrite.c>
  RewriteEngine On

  RewriteRule ^core/(src|tools|installer|bin)/ - [F,L]

  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule ^ index.php [L]
</IfModule>
